import { OurServices } from '@/components'
import React, { Suspense } from 'react'

const Services = () => {
  return (
    <Suspense>
      <OurServices/>
    </Suspense>
  )
}

export default Services